import React from 'react';

const Header = () =>{
    return(
        <header>
            <nav className="navbar">
                <h1 className="app-name">Clima app</h1>
            </nav>  
        </header>
    );
}

export default Header;